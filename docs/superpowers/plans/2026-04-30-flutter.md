# Flutter Plugin & Mobile App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the `silvestre_flutter` plugin with `flutter_rust_bridge` v2 and build an example mobile app with camera capture, a before/after split slider, and gallery export.

**Architecture:** Expose Rust filters through `flutter_rust_bridge` codegen, wrap Dart APIs into high-level classes, and implement reactive BLoC state management in Flutter.

**Tech Stack:** Dart, Flutter, Rust, `flutter_rust_bridge` v2, `flutter_bloc`

**Spec:** `docs/superpowers/specs/2026-04-30-flutter-plugin-and-app-design.md`

## Global Constraints

- Never perform heavy image transformations on the Flutter main UI isolate.
- Comply with official Flutter plugin guidelines and `flutter_lints`.

---

### Task 1: Flutter Rust Bridge Plugin Setup
**Files:**
- Create: `silvestre_flutter/pubspec.yaml`
- Create: `silvestre_flutter/rust/Cargo.toml`
- Create: `silvestre_flutter/rust/src/api/image.rs`
- Create: `silvestre_flutter/lib/silvestre.dart`

- [x] **Step 1: Define Rust bridge API methods**
- [x] **Step 2: Generate Dart bridge glue code via `flutter_rust_bridge_codegen`**
- [x] **Step 3: Implement high-level Dart wrapper API**

---

### Task 2: Flutter Example App & BLoC
**Files:**
- Create: `silvestre_flutter/example/lib/main.dart`
- Create: `silvestre_flutter/example/lib/bloc/image_bloc.dart`

- [x] **Step 1: Implement `ImageBloc` state management**
- [x] **Step 2: Build interactive before/after split comparison slider widget**
- [x] **Step 3: Integrate camera feed and photo album export**
